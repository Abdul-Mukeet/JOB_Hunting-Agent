# Core agent uses only the Python standard library.
# The following are OPTIONAL and only needed if you want to read PDF/DOCX
# files in input_jobs/, input_resumes/, or input_kb/.

PyPDF2>=3.0.0      # optional: PDF reading
python-docx>=1.1.0 # optional: DOCX resume reading
